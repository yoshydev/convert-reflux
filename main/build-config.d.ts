// このファイルはbuild-config.tsの型定義です
// 実際のbuild-config.tsはビルド時に生成されます

export interface BuildConfig {
  GOOGLE_CLIENT_ID: string;
  GOOGLE_CLIENT_SECRET: string;
}

export const BUILD_CONFIG: BuildConfig;
